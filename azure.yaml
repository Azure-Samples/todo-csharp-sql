name: todo-csharp-mongo
services:
  - name: ${AZURE_ENV_NAME}web
    project: src/web
    dist: build
    language: js
    host: appservice
  - name: ${AZURE_ENV_NAME}api
    project: src/api
    language: csharp
    host: appservice
