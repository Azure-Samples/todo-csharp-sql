# yaml-language-server: $schema=https://azuresdkreleasepreview.blob.core.windows.net/azd/schema/azure.yaml.json

name: todo-csharp-sql
metadata:
  template: todo-csharp-sql@0.0.1-beta
services:
  web:
    project: src/web
    dist: build
    language: js
    host: appservice
    resourceName: ${AZURE_ENV_NAME}web
  api:
    project: src/api
    language: csharp
    host: appservice
    resourceName: ${AZURE_ENV_NAME}api
