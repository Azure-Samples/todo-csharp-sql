name: todo-csharp-sql
metadata:
  template: todo-csharp-sql@0.0.1-beta
services:
  - name: ${AZURE_ENV_NAME}web
    project: src/web
    dist: build
    language: js
    host: appservice
  - name: ${AZURE_ENV_NAME}api
    project: src/api
    language: csharp
    host: appservice
