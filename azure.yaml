name: todo-csharp-mongo
services:
  - name: ${BASENAME}api
    project: src/api
    language: csharp
    host: appservice
  - name: ${BASENAME}web
    project: src/web
    dist: build
    language: js
    host: appservice