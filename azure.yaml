name: todo-csharp-mongo
services:
  - name: ${ENVNAME}api
    project: src/api
    language: csharp
    host: appservice
  - name: ${ENVNAME}web
    project: src/web
    dist: build
    language: js
    host: appservice