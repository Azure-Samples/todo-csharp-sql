name: todo-csharp-mongo
services:
  - name: ${AZURE_ENVNAME}api
    project: src/api
    language: csharp
    host: appservice
  - name: ${AZURE_ENVNAME}web
    project: src/web
    dist: build
    language: js
    host: appservice