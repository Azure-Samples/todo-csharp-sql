// The GroupStates describes group(todo,inprogress,done) is expanded or collapsed, eg:{todo: true,...}, todoGroup is collapsed
export interface GroupStates {
    todo:boolean
    inprogress:boolean
    done:boolean
}